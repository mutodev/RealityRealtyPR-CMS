source: FROM Branch
columns:

    name:
        label: Name
        source: name

    actions:
        label: ''
        width: 1px
        type: Actions
        actions:
            edit:
                icon: pencil
                label: Edit
                link:  .edit?id=%{id}
