source: FROM PropertyCategory
columns:

    name:
        label: Name
        source: name

    type:
        source: type

    actions:
        label: ''
        width: 1px
        type: Actions
        actions:
            edit:
                icon: pencil
                label: Edit
                link:  .edit?id=%{id}
