source: FROM Entity
columns:

    name:
        label: Name
        source: name

    type:
        source: type

    active:
        label: Active
        class: hidden-xs-down
        source: is_active
        decorator: boolean

    actions:
        label: ''
        width: 1px
        type: Actions
        actions:
            edit:
                icon: pencil
                label: Edit
                link:  .edit?id=%{id}
