source: FROM ProductionUnit
columns:

    name:
        label: Name
        source: name

    active:
        label: Active
        class: hidden-xs-down
        source: is_active
        decorator: boolean

    actions:
        label: ''
        width: 1px
        type: Actions
        actions:
            edit:
                icon: pencil
                label: Edit
                link:  .edit?id=%{id}

            goals:
                icon: line-chart
                label: Goals
                link:  backend.company.settings.goals?year=<?= date('Y') ?>&production_unit_id=%{id}
