source: FROM Organization
columns:

    id:
        label:   Id
        source:  id
        width:   30px

    name:
        label: Name
        source: name

    # Plan:
    #     label: Plan
    #     source: Organization.Subscription.Plan.chargebee_plan_id
    #     content: %{plan_name:Organization.Subscription.Plan}
    #     class: hidden-xs

    # accounts:
    #     label: <i class="fa fa-users" title="Accounts"></i>
    #     source: total_accounts
    #     decorator: Number
    #     width: 56px
    #     class: center

    # campaigns:
    #     label: <i class="fa fa-bullhorn" title="Campaigns"></i>
    #     source: total_campaigns
    #     decorator: Number
    #     width: 56px
    #     class: center

    # sources:
    #     label: <i class="fa fa-bullseye" title="Sources"></i>
    #     source: total_sources
    #     decorator: Number
    #     width: 56px
    #     class: center

    # resources_numbers:
    #     label: '<span title="Phone numbers">#</span>'
    #     source: total_resources_numbers
    #     decorator: Number
    #     width: 56px
    #     class: center

    # resources_emails:
    #     label: '<span title="Email addresses">@</span>'
    #     source: total_resources_emails
    #     decorator: Number
    #     width: 56px
    #     class: center

    active:
        label: Active
        source: is_active
        decorator: boolean
        width: 56px
        class: center

    actions:
        type: Actions
        label: ' '
        width: 1px
        actions:

            login:
                icon: 'sign-in'
                label: Login as Admin
                link:  .login?id=%{id}
                confirm: true

            subscription:
                icon: tag
                label: Plan Subscription
                link:  .subscription?id=%{id}

            numbers:
                icon: phone
                label: Add numbers
                link:  .numbers?id=%{id}
