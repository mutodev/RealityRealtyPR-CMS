source: Organization
default: advanced
fields:

    id:
        type: String
        source: og.id

    name:
        type: String
        source: og.name

    active:
        type: Boolean
        source: og.is_active
        label: Status
        element:
            type: Select
            class: form-control
            default: 1
            options:
                1: Active
                0: Inactive

